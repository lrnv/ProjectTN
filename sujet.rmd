---
title: "Untitled"
author: "Oskar LAVERNY"
date: "7 décembre 2017"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
# ISFA - M2 Actuariat — Techniques Num ́eriques

# Projet `a rendre pour le 27 janvier 2017 (avant 23h00)

## Le projet consiste en l’estimation d’uneprovision pour d ́epr ́eciation durable (PDD).

En assurance, la PDD est constitu ́ee si la valeur de march ́e de l’actif est inf ́erieure `a sa valeur d’acquisition pendant au moins 6
mois cons ́ecutifs (durable). Cette provision est calcul ́ee ligne `a ligne pour l’ensemble des actifs non obligataires. Celle-ci est reprise
en cas de vente d’actif ayant conduit `a une dotation de PDD.


Sur un exercice comptable (une ann ́ee), la dotation de PDD correspond `a l’anticipation d’une perte de valeur de l’actif de plus
de (1−α)% (α∈[0,1]) et que la perte est durable. En d’autres termes, si l’on note $S= (St)_{t≥ 0}$ l’actif avec la dynamique suivante, sous la probabilit ́e risqu nee,
dSt
St
=rdt+σdWt, (1)
```
```
et l’on suppose que ce dernier a ́et ́e acquis `a une date ant ́erieure `at= 0 au prixSa; en fin du premier exercice, c.-`a-d.t= 1,
l’assureur enregistre une d ́epr ́eciation lorsque les deux ́ev ́enements suivants se r ́ealisent :
S 1 ≤(1−α)Sa, et sup
u∈]1/ 2 ,1]
```
```
Su≤Sa.
```
```
Dans ce cas, la perte enregistr ́e est ́egale `a (Sa−S 1 ). La PDD pour cette p ́eriode (calcul ́ee en d ́ebut de p ́eriode,t= 0) est donn ́ee
par l’esp ́erance sous la probabilit ́e risque neutreQde la perte
λ 0 (r,σ,Sa,S 0 ) = (Sa−S 1 ) I{S 1 ≤(1−α)Sa}I{supu∈]1/ 2 ,1]Su≤Sa}, (2)
```
```
ou PDD 0 =EQ
```
### [

```
λ 0 (r,σ,Sa,S 0 )]. Pour la p ́eriode suivante, la perte est enregistr ́ee lorsque les deux ́ev ́enements pr ́ec ́edent (calcul ́es
sur la seconde p ́eriode) se r ́ealisent. Autrement dit, la PDD 1 est donn ́ee par l’esp ́erance de la quantit ́e suivante :
```
```
λ 1 (r,σ,Sa,S 0 ) =
```
### (

```
Sa−λ 0 (r,σ,Sa,S 0 )−S 2
```
### )+

```
I{S 2 ≤(1−α)Sa}I{supu∈]3/ 2 ,2]Su≤Sa}.
Par r ́eccurence, nous avons la perte suivante en fin de p ́eriodeT:
```
```
λT− 1 (r,σ,Sa,S 0 ) =
```
### (

### ΩT− 1 −ST

### )+

```
I{ST≤(1−α)Sa}I{supu∈]T− 1 / 2 ,T]Su≤Sa} avec ΩT− 1 =Sa−
```
### T∑− 1

```
t=
```
```
λt(r,σ,Sa,S 0 ). (3)
```
```
La provision pour d ́epr ́eciation durable pour cet actif est la suivante (sans prise en compte de l’actualisation)
```
```
PDD(r,σ,Sa,S 0 ) =EQ
```
### [T− 1

### ∑

```
t=
```
```
λt(r,σ,Sa,S 0 ).
```
### ]

### . (4)

```
L’objectif de ce qui suit est l’estimation/quantification de cette PDD.
```
## 1. Un seul actif et une seule p ́eriode.

```
On consid`ere le cas expliqu ́e supra avec des valeurs (choisies) pour initialiserr,σ,Sa,S 0 etα= 0.15 et 0.2. On estime la valeur
P[λ 0 (r,σ,Sa,S 0 )>0] ainsi que PDD 0 avec une m ́ethode de Monte-Carlo o`u la dynamique deSest discr ́etis ́ee en utilisant un
sch ́ema d’Euler (na ̈ıf) (vous choisissez un pas de discr ́etisation). L’objectif est de comparer les deux m ́ethodes.
```
## 2. Un seul actif et plusieurs p ́eriodes.

```
On consid`ere le cas expliqu ́e supra avec des valeurs (choisies) pour initialiserr,σ,Sa,S 0 etα= 0.15 et 0.2. On estime la valeur
PDD(r,σ,Sa,S 0 ) avec une m ́ethode de Monte-Carlo o`u la dynamique deSest discr ́etis ́ee en utilisant un sch ́ema d’Euler (vous
choisissez un pas de discr ́etisation). L’objectif est de comparer les deux m ́ethodes. Dans un second temps vous ́etudiez la sensibilit ́e
de la PDD par rapport `ar,σ SaetS 0.
```
```
1.1 Estimation de la PDD.
```
M ́ethode na ̈ıve.Il s’agit d’une simple impl ́ementation de la m ́ethode de Monte-Carlo o`u supu∈]t− 1 / 2 ,t]Suest calcul ́e sur les�el ́ements de la trajectoires discr`ete deS.

```
M ́ethode raffin ́ee.Ici, il s’agit d’un raffinement naturel de la m ́ethode na ̈ıve consistant en l’utilisation d’unpont Brownienpour
quantifier la probabilit ́e de franchissement de la barri`ereSaentre deux ́el ́ements cons ́ecutifs de la grille de discr ́etisation de la
trajectoire deS.
```
1. Il faut consid ́erer les cas suivants :T= 1, 2 , 5 , 10 , 15
2. Il faut tracer l’ ́evolution de la PDD en fonction deretσpourT= 1 etT= 10.

```
1.2 La sensibilit ́e de la PDD.
Ici, il s’agit de quantifier la sensibilit ́e de la PDD par rapport `a la volatilit ́eσ, le taux d’int ́erˆet sans risqueret la valeur initiale de
l’actifS 0. En d’autres termes, nous avons besoin d’approcher les quantit ́es suivantes :
```
```
Vega =
```
### ∂

```
∂σ
```
### EQ

### [

```
λ 0 (r,σ,Sa,S 0 )], Rho =
```
### ∂

```
∂r
```
### EQ

### [

```
λ 0 (r,σ,Sa,S 0 )], Delta =
```
### ∂

### ∂S 0

### EQ

### [

```
λ 0 (r,σ,Sa,S 0 )]. (5)
```
```
Vous pouvez utiliser, par exemple, une m ́ethode de diff ́erence finies.
1
```

1. Il faut tracer l’ ́evolution des sensibilit ́es de la PDD en fonction der,σetS 0

## 3. Deux actifs.Il s’agit de calculer la PDD quand l’assureur d ́etient deux actifs. Il faut consid ́erer les deux cas de figures

suivants. Les deux actifs sont ind ́ependantρ= 0, les deux actifs sontcomonotonesρ= 1, les deux actifs sont corr ́el ́es avec une
corr ́elationρ=− 0 .3 (on utilisera l’algorithme deCholeskyet non pas la fonctionmvrnomrdeRpour les simulations).

Il faut rendre un document expliquant et discutant bri`evement les r ́esultants (d’une 15aine de pages) ainsi
qu’un seul et uniquefichier source pour les codes (R, SAS, VBA, Matlab, C, C++ ou autres).

Le projet est `a rendre pour le 27 janvier 2017 avant 23h00. Le rapport ainsi que le fichier source du code
doivent ˆetre nom ́es de la fa ̧con suivante ”NumNom1Nom2Nom3.extension”, o`u ”Num” ́etant le num ́ero du
groupe. L’objet du mail doit ˆetre [Projet TechNum 2016].

R ́ef ́erence (Cas similaire).J. Azzaz, S. Loisel, P.-E. Th ́erond,Some characteristics of an equity security next-year impairment,
Review of Quantitative Finance & Accounting, 02/

### 2


